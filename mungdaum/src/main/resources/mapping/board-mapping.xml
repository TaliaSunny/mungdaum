<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="BoardDAO">
  	<insert id="insertBoard">
  		insert into board(seq, title, writer, content) <!-- 속성 = # {값}  -->
  		values((select NVL(MAX(seq),0)+1 from board), #{title}, #{writer}, #{content})<!-- #을 사용하는 이유 보안에 좋기때문이다. -->
  	</insert>
  	
  	<select id="getBoardList" resultType="board"> <!-- 전체 게시글 select하는 sql문장 -->
  		select * from board order by seq desc
  	</select>
  	
  	<select id="getBoard" resultType="board"> <!-- select일때만 들어감. -->
  		select * from board where seq=#{seq}
  	</select>
  	
  	<!-- update -->
  	<update id="updateBoard">
  		update board set title=#{title}, content=#{content} where seq=#{seq}
  	</update>
  	
  	<delete id="deleteBoard">
  		delete board where seq=#{seq}
  	</delete>
  </mapper>